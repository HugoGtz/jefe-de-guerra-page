# Cloudflare Pages config (used by `wrangler pages deploy`).
# `name` is the Pages project; `pages_build_output_dir` is the static build.
# The functions/ directory is auto-detected and deployed as Pages Functions.
name = "jefe-de-guerra"
pages_build_output_dir = "build"
compatibility_date = "2025-01-01"
